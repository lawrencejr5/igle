import { Request, Response } from "express";

import Transaction from "../models/transaction";

export const make_transaction = (req: Request, res: Response) => {
  try {
  } catch (err) {
    console.log(err);
  }
};
